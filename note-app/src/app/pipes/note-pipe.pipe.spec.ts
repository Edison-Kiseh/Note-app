import { NotePipePipe } from './note-pipe.pipe';

describe('NotePipePipe', () => {
  it('create an instance', () => {
    const pipe = new NotePipePipe();
    expect(pipe).toBeTruthy();
  });
});
